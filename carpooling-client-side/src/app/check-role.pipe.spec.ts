import { CheckRolePipe } from './check-role.pipe';

describe('CheckRolePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckRolePipe();
    expect(pipe).toBeTruthy();
  });
});
