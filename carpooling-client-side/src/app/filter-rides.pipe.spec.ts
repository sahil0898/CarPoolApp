import { FilterRidesPipe } from './filter-rides.pipe';

describe('FilterRidesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRidesPipe();
    expect(pipe).toBeTruthy();
  });
});
